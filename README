aws s3 sync s3://tripdata data/zip --no-sign-request

python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
time python fixup.py

time loadup.sh



TODO:
* combine user_type and member_casual?
* duration column?